<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personal.quantization.inventory.mapper.InventoryMapper">
  
    <update id="reduceInventory">
        update inventory set inventory = (inventory -1), thread_name = #{threadName}, add_time = now(), update_time = now() where product_id = #{productId}
    </update>

</mapper>